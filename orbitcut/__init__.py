"""OrbitCut — GoPro mountain-bike footage pipeline."""
# Bumped whenever the on-disk code changes in a way you would want to
# confirm is actually loaded. `orbitcut doctor` prints this together with
# the path it was imported from, which is the fastest way to catch an edit
# that landed somewhere other than the copy being run.
__version__ = "0.2.0"
